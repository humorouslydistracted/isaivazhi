<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose-only app: keep the parent platform theme minimal so it
         doesn't conflict with Material 3 Compose colors. -->
    <style name="Theme.IsaiVazhi" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Splash theme: same window background so the cold start doesn't flash. -->
    <style name="Theme.IsaiVazhi.Splash" parent="Theme.IsaiVazhi">
        <item name="android:windowDisablePreview">false</item>
    </style>
</resources>
